name: spare_parts_inventory
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - uv
      - -r requirements.lock
      - ruff
      - black
      - mypy
      - pytest
      - pytest-asyncio
      - coverage
